<?xml version="1.0" encoding="utf-8"?>
<views:MvxContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             xmlns:views="clr-namespace:MvvmCross.Forms.Views;assembly=MvvmCross.Forms" 
             x:Class="Assignment3.Views.AboutPage" >

 <ContentPage.Content>    
        <ListView x:Name="JobList" HasUnevenRows="True" 
                  ItemsSource="{Binding Jobs}" RowHeight="120">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <StackLayout Orientation="Horizontal" Padding="10">
                            <Label Text="{Binding JobTitle}" VerticalOptions="Center" FontSize="Small" WidthRequest="100" />
                            <Label Text="{Binding JobLocation}" FontSize="Small" TextColor="Gray" HorizontalOptions="EndAndExpand" VerticalOptions="Center" />
                        </StackLayout>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </ContentPage.Content>
</views:MvxContentPage>